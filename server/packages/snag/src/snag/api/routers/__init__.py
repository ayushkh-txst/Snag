"""Every module in this package is auto-discovered by `snag.api.app.create_app`
(via `_include_routers`) and mounted under `/api` — a new file with a
module-level `router` is all a new endpoint set needs.
"""
